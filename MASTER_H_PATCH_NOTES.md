# MASTER H — FreightLink Cohesion Patch

Locked FreightLink flow:
- Home filters: Available / Active / History.
- Available freight supports List / Map.
- Map pickup pins preview one load at a time and draw its pickup-to-delivery lane.
- Map preview converges on the same Load Detail screen via VIEW LOAD.
- No alternate accept path and no automatic dispatch behavior.
- Load detail header is compact and route summary stays on one line.
- Driver Assignment is now a compact row, not a card; PLAN TRIP remains the next explicit action when assignment is ready.
- Planning still returns operational dispatch ownership to the DOC OS map.
- Multi-load Build Route is intentionally deferred.
