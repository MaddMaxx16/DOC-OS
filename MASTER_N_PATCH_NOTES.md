# MASTER N PATCH NOTES

## FreightLink final polish
- Driver map popup now auto-closes after ~2.2 seconds, matching the lightweight map-card behavior established for facilities.
- Driver Select page now uses `Driver Select` as the primary header instead of `Assign DOC###`.
- Load identity remains visible in the smaller kicker (`FREIGHTLINK · DOC###`) so context is preserved without oversized copy.
- No load lifecycle, assignment, planning, dispatch, routing, or tutorial logic changed.
