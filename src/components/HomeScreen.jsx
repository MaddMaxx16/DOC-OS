function HomeScreen({ onOpenBrowser, onOpenDocuments, onOpenLedger, onOpenEmail, emailBadgeCount = 0, documentsBadgeCount = 0, ledgerUnreadCount = 0, tutorialTarget = null }) {
  return <div className="phone-page home-screen"><h1>DOC OS</h1><div className="phone-app-grid"><button type="button" className="app-icon" onClick={onOpenBrowser}><div className="app-icon-square">W</div><span>Browser</span></button><button type="button" className="app-icon" onClick={onOpenDocuments}><div className={`app-icon-square ${tutorialTarget === 'documents-app' ? 'tutorial-target' : ''}`}>D</div><span>Documents</span>{documentsBadgeCount > 0 && <span className="app-icon-badge">{documentsBadgeCount}</span>}</button><button type="button" className="app-icon" onClick={onOpenLedger}><div className={`app-icon-square ${tutorialTarget === 'ledger-app' ? 'tutorial-target' : ''}`}>L</div><span>LedgerDesk</span>{ledgerUnreadCount > 0 && <span className="app-icon-badge ledger-badge">{ledgerUnreadCount > 9 ? '9+' : ledgerUnreadCount}</span>}</button><button type="button" className="app-icon" onClick={onOpenEmail}><div className={`app-icon-square ${tutorialTarget === 'email-app' ? 'tutorial-target' : ''}`}>E</div><span>Email</span>{emailBadgeCount > 0 && <span className="app-icon-badge">{emailBadgeCount}</span>}</button></div></div>
}
export default HomeScreen
