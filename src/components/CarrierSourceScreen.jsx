function CarrierSourceScreen({ carrier, application, onOpen }) { if (!carrier) return <div className="phone-page"><h2>CARRIERSOURCE</h2><p>Carrier opportunities are loading.</p></div>; const state = application?.status || (carrier.status === 'active' ? 'ACCEPTED' : null); return <div className="phone-page"><h2>CARRIERSOURCE</h2><p>Find Clients</p><button className="document-card" type="button" onClick={onOpen}><strong>{carrier.name}</strong><span>{state === 'PENDING' ? 'APPLICATION PENDING' : state === 'OFFER_RECEIVED' ? 'OFFER RECEIVED' : state === 'ACCEPTED' ? 'CLIENT ACTIVE' : 'Seeking Dispatch Service'}</span><span>Brooklyn, NY</span><span>Fleet: 1 Driver · Equipment: 53' Dry Van</span><span>Service Area: Northeast · Agreement: 8%</span><em>{state === 'ACCEPTED' ? 'CLIENT ACTIVE' : state === 'PENDING' ? 'PENDING REVIEW' : state === 'OFFER_RECEIVED' ? 'OFFER RECEIVED' : 'VIEW OPPORTUNITY'}</em></button></div> }
export default CarrierSourceScreen
