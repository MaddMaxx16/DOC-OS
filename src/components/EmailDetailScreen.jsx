import { formatCompactDate, formatTime } from '../utils/gameTime.js'
import { mentorMessages } from '../data/tutorialContent.js'
function EmailDetailScreen({ message, carrier, onReview, onBack, tutorialTarget = null }) { if (!message) return <div className="phone-page"><p>Message unavailable.</p><button type="button" className="back-button email-back-button" onClick={onBack}>BACK</button></div>; const template = mentorMessages[message.templateId]; const isMentor = message.type === 'mentor'; return <div className="phone-page"><div className="screen-header"><button type="button" className="back-button email-back-button" onClick={onBack}>‹</button><h2>EMAIL</h2></div><p>FROM<br />{template?.sender || carrier?.name}</p><p>SUBJECT<br />{template?.subject || message.subject}</p><p>RECEIVED<br />{formatCompactDate(Math.floor(message.receivedGameMinute / 1440))} • {formatTime(message.receivedGameMinute % 1440)}</p><p>{template?.body || 'We reviewed your application and would like to move forward with dispatch services.'}</p>{!isMentor && <><h3>PROPOSED AGREEMENT</h3><p>Dispatch Fee<br />8%</p><p>Payment Terms<br />1 Day</p></>}{template?.action === 'OPEN CARRIERSOURCE' && <button type="button" className={`action-button ${tutorialTarget === 'open-carriersource' ? 'tutorial-target' : ''}`} onClick={() => onReview('carrierSource')}>OPEN CARRIERSOURCE</button>}{template?.action === 'OPEN FREIGHTLINK' && <button type="button" className={`action-button ${tutorialTarget === 'open-freightlink' ? 'tutorial-target' : ''}`} onClick={() => onReview('freightlink')}>OPEN FREIGHTLINK</button>}{!isMentor && !template && <button type="button" className={`action-button ${tutorialTarget === 'review-agreement' ? 'tutorial-target' : ''}`} onClick={() => onReview('agreement')}>REVIEW AGREEMENT</button>}</div> }
export default EmailDetailScreen
