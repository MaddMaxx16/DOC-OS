import { mentorMessages } from '../data/tutorialContent.js'
function EmailScreen({ messages, carriers, onOpenMessage, onBack, tutorialTarget = null }) {
  return <div className="phone-page"><div className="screen-header"><button type="button" className="back-button email-back-button" onClick={onBack}>‹</button><h2>EMAIL</h2></div><p>Inbox</p>{messages.length === 0 && <p>No messages.</p>}{[...messages].sort((a, b) => b.receivedGameMinute - a.receivedGameMinute).map((message) => { const carrier = carriers.find((item) => item.id === message.carrierId); const template = message.type === 'mentor' ? mentorMessages[message.templateId] : null; return <button type="button" className={`document-card ${message.read ? '' : 'unread'} ${message.id === tutorialTarget?.split(':')[1] ? 'tutorial-target' : ''}`} key={message.id} onClick={() => onOpenMessage(message)}><strong>{template?.sender || carrier?.name || message.carrierId}</strong><span>{template?.subject || message.subject || ''}</span><small>{message.read ? '' : 'Unread'}</small></button> })}</div>
}
export default EmailScreen
